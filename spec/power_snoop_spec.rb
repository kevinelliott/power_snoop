require 'spec_helper'

describe PowerSnoop do
  it 'has a version number' do
    expect(PowerSnoop::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
